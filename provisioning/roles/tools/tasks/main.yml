---
- name: apt-get update
  apt:
    update_cache: yes
    cache_valid_time: 3600

#- name: Update all packages to the latest version
#  apt:
#   upgrade: dist

- name: Install Generic Tools
  become: yes
  apt: 
    name: ['htop', 'vim', 'git', 'iftop', 'acl', 'apache2-utils', 'nmap', 'whois']
    state: latest
...
